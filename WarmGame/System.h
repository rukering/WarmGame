void StartTitleScene(void); // 타이틀 씬을 그리기 위해 한번 실행
void UpdateTitleScene(void); // 타이틀 씬에 있는 동안 반복해 실행. 플레이어 입력 받음.


// 인게임 씬
void StartGameScene(void); // 게임 초기 세팅, 로직 초기화
void UpdateGameScene(void); // 게임 로직 업데이트

// 결과 씬
void GameOverScene(void); // 게임 종료 씬으로 넘어감.

// 일시정지
void Pause(void);  // 일시 정지


// 유저 입력 업데이트
void GetUserInput(void);

// 타이틀 씬
void StartTitleScene(void); // 타이틀 씬을 그리기 위해 한번 실행
void UpdateTitleScene(void); // 타이틀 씬에 있는 동안 반복해 실행. 플레이어 입력 받음.

// 인게임 씬
void StartGameScene(void); // 게임 초기 세팅, 로직 초기화
void UpdateGameScene(void); // 게임 로직 업데이트

// 결과 씬
void GameOverScene(void); // 게임 종료 씬으로 넘어감.

// 일시정지
void Pause(void);  // 일시 정지

// 유저 입력 업데이트
void GetUserInput(void);